import 'package:flutter/material.dart';

class RaceTraitsView extends StatelessWidget {
  const RaceTraitsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
